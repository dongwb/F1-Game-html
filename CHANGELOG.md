# 版本变动记录

## v5.2 — 赛车外观重绘（2026-07-21）

`drawCar()` 重绘为俯视卡通造型（参考卡通 F1 插画）：

- 宽前翼：车头最前方的深灰圆角横翼，宽于轮距
- 细长鼻锥连接前翼与座舱，尖端带 accent 饰条
- 深色椭圆座舱 + 头盔色点，取代原 Halo 结构
- 车身改为座舱后方向两侧外扩的侧箱轮廓，后轮前内凹收腰
- 轮胎改为圆角矩形，前轮小、后轮大，取代原椭圆轮胎
- 尾翼改为深灰圆角横翼；保留白色车号、accent 中央拉花、刹车红色光晕
- 函数签名与整体尺寸（约 ±46 × ±26）不变，HUD 小图标 / 尾流 / 碰撞等调用点无需改动
- `PROMPT.md` 渲染章节同步更新

## v5.1 — 尾流空气动力学优化（2026-07-20）

弯道尾流圆弧化：尾流不再沿当前朝向画直线，而是沿赛车最近的真实轨迹延伸，弯道中自然弯曲成圆弧，符合空气动力学。

- 每辆车新增 `trail` 轨迹历史（位置 + 朝向，最多 40 个采样点，主循环每帧记录）
- 新增 `buildWakeRibbon()`：以轨迹上各时刻的车尾位置为尾流中心线，相邻点中心差分求法线，半宽由车尾到尾端渐缩
- 新增 `fillWakeRibbon()`：逐段填充条带，透明度沿弧长衰减
- `drawWake` / `drawSlipstream` 改用弯曲条带渲染；滑流辉光粒子沿圆弧散布
- 触发条件、长度、宽度、颜色等参数与 v5.0 完全一致；直线行驶时视觉效果与旧版一致
- 冲出赛道重设时清空 `trail`，避免尾流横跨瞬移路径
- `drawDRS` 绿色尾焰保持不变
- `PROMPT.md` 同步更新（车辆属性 + 渲染章节）

## v5.0 — 首个正式标签版本（2026-07-20）

- 15 条无自交真实 F1 赛道（meijersa/f1-circuits GeoJSON 数据集）
- DRS 减阻系统（P1 右 Option / P2 空格），干净圈冷却减半机制
- 6 级等距 AI 难度梯度（0.90×~1.40× 极速，80%~100% 过弯）
- 尾流/滑流极速加成、单双人模式、分屏双相机、赛道选择器分页
- 赛道生成流水线：GPS → 平滑 → 重采样 → Catmull-Rom 样条

---

更早版本见 git 历史标签：`v4-milestone` / `v3` / `v2` / `v1`
