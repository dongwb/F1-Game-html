# F1 Grand Prix — F1 赛车游戏全栈实现

## 项目概述

一个基于 Canvas 的 F1 赛车游戏，支持双人对战、AI 对战、6 级难度、完整赛道圈数统计、发车灯倒计时等真实赛车要素。全部代码包含在单个 `index.html` 文件中。

- **技术栈**: 纯前端 HTML5 Canvas + JavaScript（无外部依赖）
- **文件结构**: 单文件（约 1730 行）
- **核心玩法**: 方向盘控制、速度/档位模拟、圈数计时、赛道边缘检测与重设
- **AI 系统**: 基于 Pure Pursuit + 曲率速度剖面 + 预判反打（Scandinavian Flick）的成熟赛车 AI

---

## 目录

1. [配置系统](#1-配置系统)
2. [赛道生成与几何](#2-赛道生成与几何)
3. [赛车物理](#3-赛车物理)
4. [玩家控制](#4-玩家控制)
5. [AI 驾驶系统](#5-ai-驾驶系统)
6. [发车与比赛流程](#6-发车与比赛流程)
7. [碰撞系统](#7-碰撞系统)
8. [渲染系统](#8-渲染系统)
9. [HUD 与 UI](#9-hud-与-ui)
10. [开发流程与演进历史](#10-开发流程与演进历史)

---

## 1. 配置系统

### 物理常量 (`CFG`)

```javascript
const CFG = {
  trackWidth: 220,    // 赛道宽度（像素）
  maxSpeed: 450,      // 最高速度
  accel: 220,         // 加速度
  brake: 380,         // 刹车减速度
  drag: 40,           // 空气阻力
  offTrackDrag: 500,  // 冲出赛道后的额外阻力
  turnLow: 3.0,       // 低速转向灵敏度
  turnHigh: 0.85,     // 高速转向灵敏度
};
```

### 难度等级 (`DIFFICULTY`)

6 级难度通过 5 个参数调节 AI 性能：

| 等级 | speedMult | errorScale | brakeFactor | accelFactor | cornerSpeed |
|------|-----------|------------|-------------|-------------|-------------|
| 简单 | 0.70 | 1.5 | 0.55 | 0.75 | 0.25 |
| 中等 | 0.90 | 0.6 | 0.75 | 0.90 | 0.38 |
| 困难 | 1.05 | 0.18 | 0.90 | 1.05 | 0.48 |
| 超级困难 | 1.18 | 0.05 | 1.0 | 1.15 | 0.58 |
| 地狱级 | 1.32 | 0.01 | 1.0 | 1.25 | 0.68 |
| BOSS | 1.45 | 0.001 | 1.0 | 1.35 | 0.78 |

- `speedMult`: 最高速度倍率（受限于 400 px/s 硬上限）
- `errorScale`: AI 转向误差幅度（低级 AI 转向更不稳定）
- `brakeFactor` / `accelFactor`: 刹车/加速效率倍率
- `cornerSpeed`: 过弯速度系数（影响速度剖面的缩放）

### 游戏设置

```javascript
const gameSettings = {
  mode: 'single',   // 'single' | 'dual' — 单人/双人
  aiCount: 1,       // 0 | 1 | 2 — AI 对手数量
};
```

---

## 2. 赛道生成与几何

### 路径点与样条插值

赛道由 16 个路径点（`waypoints`）定义，呈环形布局，包含 4 段直道和 4 组弯道（S 形弯 + 减速弯交替）：

```
底部（向右）→ 右侧（向上，S弯）→ 顶部（向左）→ 左侧（向下，S弯）→ 回到底部
```

使用 Catmull-Rom 样条插值（`catmullRom`），每段 24 个细分点，生成约 400 个赛道点。

```javascript
function catmullRom(t, p0, p1, p2, p3) {
  const t2 = t * t, t3 = t2 * t;
  return 0.5 * ((2 * p1) + (-p0 + p2) * t + (2 * p0 - 5 * p1 + 4 * p2 - p3) * t2 + (-p0 + 3 * p1 - 3 * p2 + p3) * t3);
}
```

### 赛道点属性

`track` 数组中每个点包含：

| 属性 | 含义 |
|------|------|
| `.x` / `.y` | 世界坐标 |
| `.tx` / `.ty` | 切线方向（单位向量） |
| `.nx` / `.ny` | 法线方向（指向赛道外侧） |
| `.dist` | 从起点到该点的累计路径长度 |
| `.totalLen` | 赛道总周长 |

### 最近赛道点查询

两步搜索策略——粗搜索（步长 3）→ 精搜索（±4 邻域）：

```javascript
function nearestTrackPoint(x, y) {
  let best = 0, bd = Infinity;
  for (let i = 0; i < track.length; i += 3) { ... }
  const s = Math.max(0, best - 4), e = Math.min(track.length, best + 5);
  for (let i = s; i < e; i++) { ... }
  return { idx: best, dist: Math.sqrt(bd), point: track[best] };
}
```

返回：索引、距离、赛道点引用。

---

## 3. 赛车物理

### 赛车属性

每辆赛车是一个对象，关键属性：

```javascript
{
  x, y,             // 位置
  speed,            // 当前速度
  heading,          // 朝向（弧度，0=向上）
  lapCount,         // 已完成圈数
  onTrack,          // 是否在赛道上
  blocked,          // 是否被锁定（比赛开始前/结束后）
  braking,          // 刹车灯状态
  color, accent,    // 外观（车身色 + 翼/条纹强调色）
  // 配色方案 (F1 车队灵感):
  //   p1 #16: 法拉利红 #dc0000 + 金色 #ffd700
  //   p2 #01: 梅赛德斯青 #00d2be + 银 #c0c0c0
  //   a1 #44: 红牛蓝 #1e3a5f + 红 #ff1e00
  //   a2 #77: 迈凯伦木瓜橙 #ff7300 + 深蓝 #002147
  num,              // 车号
  offTrackTimer,    // 冲出赛道计时（>1秒触发重设）
}
```

AI 额外属性：`trackIdx`, `errDrift`, `errTimer`, `prevSpeed`, `entryPathAlpha`, `entryPath`

### 移动公式

每帧更新：

```javascript
car.x += Math.sin(car.heading) * car.speed * dt;
car.y -= Math.cos(car.heading) * car.speed * dt;
```

坐标系：`heading=0` 时车头朝上，x 正方向为右，y 正方向为下。

### 速度控制

- **加速**: `speed = min(speed + accel * dt, maxSpeed)`
- **刹车**: `speed = max(speed - brake * dt, 0)`
- **空气阻力**: `speed -= drag * (speed/maxSpeed) * dt`（与速度比成比例）
- **冲出赛道阻力**: `speed -= offTrackDrag * dt`（强力减速）

### 转向

转向灵敏度随速度变化：

```javascript
const turn = turnLow + (turnHigh - turnLow) * Math.min(speed / maxSpeed, 1);
// 低速时转向灵 (3.0)，高速时转向迟钝 (0.85)
```

### 冲出赛道与重设

当赛车中心到赛道中心线距离 `> trackWidth * 0.55`（即 121px）时视为 off-track：

1. 立即施加 `offTrackDrag`（500 px/s²）减速
2. 启动 `offTrackTimer` 计时
3. 超过 1 秒后自动重设到最近赛道点（位置 + 朝向 + 重置速度为 50）

玩家和 AI 完全共享此逻辑。

---

## 4. 玩家控制

### 按键映射

- **玩家 1**: 方向键（↑ 加速 / ↓ 刹车 / ← → 转向）
- **玩家 2**（双人模式）: WASD（W 加速 / S 刹车 / A D 转向）

### 更新逻辑（`updatePlayer`）

1. **油门/刹车**: 根据按键加减速，支持倒车
2. **空气阻力**: 持续作用
3. **转向**: 速度相关灵敏度
4. **冲出赛道**: 减速 + 计时（由主循环处理重设）
5. **刹车灯**: 刹车且速度 > 30 时亮起
6. **轮胎痕迹**: 刹车或高速转向时留下（仅玩家 1）

---

## 5. AI 驾驶系统

### 总体架构

`updateAI(car, dt)` 包含 5 个阶段：

```
1. 赛道位置追踪
2. Pure Pursuit 中心线追踪
3. 预判性预转向 + 反打（Scandinavian Flick）
4. 横向偏移预测修正
5. 边缘安全网
6. 速度控制
7. 位置更新
```

### 5.1 赛道位置追踪

跟踪 AI 在赛道上的当前位置索引，带环绕修正：

```javascript
let idxDiff = n.idx - car.trackIdx;
if (idxDiff < -track.length / 2) idxDiff += track.length;
if (idxDiff > track.length / 2) idxDiff -= track.length;
if (Math.abs(idxDiff) < track.length / 3) car.trackIdx = n.idx;
```

允许小幅后退修正，防止急弯中索引跳跃。

### 5.2 Pure Pursuit 中心线追踪（含弯心瞄准）

核心转向逻辑：跟踪前方一定距离的目标点。直道上使用中心线，弯道中通过 **弯心瞄准（Apex Targeting）** 将目标点移向内侧，实现真实赛车手的 out-in-out 走线。

```javascript
// 扫描前方弯道最急点（Apex），将目标向内侧偏移
const searchEnd = Math.min(predSteps, 90);
for (let i = 5; i < searchEnd; i++) {
  const idx = (car.trackIdx + i) % track.length;
  const cross = Math.abs(track[idx].tx * track[next].ty - track[idx].ty * track[next].tx);
  if (cross > bestCross) { bestCross = cross; bestApexIdx = idx; }
}
// 在弯心处构建内侧目标点
apexTarget = {
  x: track[apexIdx].x + track[apexIdx].nx * insideDir * apexOffset,
  y: track[apexIdx].y + track[apexIdx].ny * insideDir * apexOffset,
};
```

- 使用赛道切线的**叉积**检测弯道最急点（曲率最大处）
- 在弯心处将目标点向内偏移（最多 30% 赛道宽度）
- 低速时看 15 步，高速时看 50 步
- 计算目标点与车辆的夹角误差 `headingErr`
- 转向增益：`err > 60°` 时 2.8，否则 1.5
- 最大转向速率：`7.0 * dt * gain`
- 加入模拟漂移 `errDrift`（随机误差，等级越高漂移越小）

### 5.3 预判性预转向

#### 5 秒前瞻

```javascript
const predSteps = Math.floor(Math.min(car.speed * 5.0 / avgSegLen, track.length * 0.35));
```

根据当前速度计算 5 秒后到达的赛道索引（原为 3 秒，v3.2 增至 5 秒以提高预见性）。预测赛道方向与当前车头的夹角 `predErr`（限制在 ±0.6 弧度 ≈ ±34°）。

#### 反打技术（Scandinavian Flick）

当 `|predErr| > 0.25` 且 `speed > 200` 时激活，用于急弯：

**Phase 1 — 反打（向赛道外侧行驶）**

- 左弯向右打 → 赛车靠右行驶
- 右弯向左打 → 赛车靠左行驶
- 目标：为入弯创造更大的转向空间
- 阈值：车身中心到赛道外侧的距离 `outsideDist`

**Phase 2 — 入弯（向弯心切入）**

- 到达外侧后向弯内转向
- **弯心瞄准（Apex Targeting）**：检测到弯道顶点时，直接向内侧偏移目标点转向，而非使用抽象的 `predErr × gain`
  ```javascript
  // 向弯心目标点转向（替代 predErr * gain）
  car.heading += sign(apexErr) * min(|apexErr|, 5.0 * dt) * 2.2;
  ```
- **出弯跟踪（Track-out）**：弯心过后，自动切换到出弯阶段，瞄准弯道出口外侧边缘
  - 在最佳弯心点后约 25 个采样点处构建出口目标点
  - 出口目标向赛道外侧偏移 35% 赛道宽度，实现 smooth exit
  - 增益系数 0.6-1.2（取决于弯道急缓），低于入弯增益以保证出弯平顺
  ```javascript
  // 出弯时向外侧出口目标转向
  exitGain = 0.6 + cornerSharpness * 0.6;
  heading += sign(exitErr) * min(|exitErr|, 2.5 * dt) * exitGain;
  ```
- 回退策略：若未检测到弯心目标，仍使用 `predErr × turnGain` 转向
- 转向增益和外侧摆动幅度根据弯道急缓程度自适应：
  - 使用 `speedProfile` 扫描前方路面
  - 弯越急（剖面速度低），摆动越靠外（最大 36% 赛道宽度），转向越猛（最大增益 0.30）
  - 缓弯：18% 宽度 + 0.15 增益
  - 急弯：36% 宽度 + 0.30 增益（保留约半个车宽的安全距离）

**弯道急缓自适应系数**

```javascript
const cornerSharpness = Math.max(0, Math.min(1, 1 - minSpd / (CFG.maxSpeed * 0.4)));
const swingThreshold = 0.18 + cornerSharpness * 0.18;  // 0.18 → 0.36
const turnGain = 0.15 + cornerSharpness * 0.15;        // 0.15 → 0.30
```

#### 激进入弯模式

反打 Phase 2 期间 `cornerSharpness > 0.4` 时激活 `car._aggressiveCorner = true`：
- **跳过** 2 秒横向预测修正（不干扰入弯转向）
- 边缘安全阈值从 38% 放宽到 44%（允许车身更贴近边缘，但保留约半个车宽余量）
- 完全依赖 off-track 系统（55% + 1s 重设）做最终边界保护

### 5.4 横向偏移预测修正

独立于预转向系统，预测 2 秒后位置。如果预测位置超出赛道宽度 25%，施加修正转向：

```javascript
const predX = car.x + Math.sin(car.heading) * car.speed * 2.0;
const predY = car.y - Math.cos(car.heading) * car.speed * 2.0;
```

激进入弯时跳过此项修正。

### 5.5 边缘安全网

检测车身四角（前后中心 + 左右后轮位置，22px/14px 偏移）到赛道边缘的距离。任意检测点超出 38%（激进入弯时 44%）时施加反向转向。包含后轮位置检测确保轮胎不滑出赛道。

```javascript
const edgeThresh = CFG.trackWidth * (car._aggressiveCorner ? 0.44 : 0.38);
// 检测点：前中、后中、右后轮、左后轮
```

### 5.6 速度控制

基于 **Menger 曲率速度剖面**的高级速度控制：

1. **扫描前方**: 向前 20-90 个点查找最低剖面速度
2. **难度缩放**: `minProfileSpeed * (cornerSpeed / 0.78)`
3. **目标速度混合**: 直道 max 与弯道 profile 的加权混合
4. **制动区**: 当前速度超过剖面速度 1.2 倍时强制刹车
5. **循迹刹车（Trail Braking）**: 入弯瞄准弯心时（`apexTarget && !apexReached`），刹车力度降至 55%，模拟真实赛车手入弯时逐步释放刹车的技术，使转向更平滑
6. **防甩尾**: heading error > 90° 时将目标速度降至 15%
6. **跑宽减速**: `runWideUrgency > 0` 时目标速度降 0%~35%

---

## 6. 速度剖面预计算

### Menger 曲率

在赛道中心线上，用 3 点窗口（间距 5）计算曲率：

```javascript
const area = Math.sqrt(Math.max(0, s*(s-a)*(s-b)*(s-c)));
curv[i] = 4 * area / (a * b * c);
```

叉积判断曲率方向（左弯为正、右弯为负）。

### 前进-后退最小时间算法（Forward-Backward Min-Time Pass）

这是赛车游戏中成熟的弯道速度计算方法：

1. **曲率限制速度**: `vCurv[i] = sqrt(latAccel / |curv[i]|)`，限制 `latAccel = 500 px/s²`
2. **前进扫描**（加速限制）: 从起点向后，逐点限制 `v[i] <= sqrt(v[i-1]² + 2 * accel * segLen)`
3. **后退扫描**（刹车限制）: 从终点向前，逐点限制 `v[i] <= sqrt(v[i+1]² + 2 * brake * segLen)`
4. **平滑处理**: 3 次滑动窗口平均

结果：`speedProfile[i]` 表示第 i 个赛道点的理论最高安全速度。

---

## 7. 发车与比赛流程

### 发车格

```
第0排：P1（左）  P2/AI#44（右）
第1排：AI#44/#77（左）  AI#77（右）
```

排间距 126px，列间距 56px。赛车方向与赛道起始点切线对齐。

### 倒计时

5 个红灯逐一亮起（间隔 0.6 秒），全部亮起后等待 1.2 秒，绿灯亮起比赛开始。

### 圈数检测

基于赛道环绕检测：赛车从赛道末端（>70%）穿越到起点（<30%）标记环绕，经过终点线（`finishIdx`）时计圈。冷却时间 1.5 秒防止重复计圈。

### 完赛条件

任意赛车完成设定圈数（3/5/8/10 圈可选）后，比赛结束并显示排名。

---

## 8. 碰撞系统

### 弹性碰撞

检测两车距离 < 38px：
- 沿法线方向推开（重叠量 × 0.6）
- 速度交换带阻尼（动量 × 0.12 / × 0.6）
- 碰撞位置产生火花效果

---

## 9. 渲染系统

### 赛道渲染

- **路面**: 深灰色填充，白色虚线标边缘
- **路肩**: 红白交替条纹（曲率变化点）
- **发车线/终点线**: 黑白棋盘格
- **计时器旗杆**: 红色三角旗

### 赛车渲染

- 完整 F1 车体绘制（端板、前翼、尾翼、Halo、中线、号码）
- 车轮带纹理
- 刹车灯：尾部红色发光效果
- 缩放系数 `scale`：渲染小车用于发车格

### 气动尾流（`drawWake`）

速度 > 300 时，车尾绘制渐变半透明气流线条：

```javascript
const intensity = Math.min(1, (car.speed - 300) / 150);
```

- 长度：30 + `intensity` × 50 像素
- 宽度：8 + `intensity` × 10 像素
- 梯形渐变为蓝色半透明

### 速度线（`drawSpeedLines`）

速度 > 120 时，车侧随机线条模拟高速感。

### 入场路径

Phase 2 入弯时同色的半透明路径线，不显示时自动淡出。

### 草地、轮胎痕迹、火花

- 草地：4000 个随机草叶 + 径向渐变
- 轮胎痕迹：最多 2500 条，逐渐透明
- **刹车痕迹**：所有赛车刹车时在赛道留下黑色印记
  - 印记最大数量 3000，每帧在左右后轮位置各添加一条（间隔 15px）
  - 保留 5 秒，前 3 秒全透明，后 2 秒逐渐淡化消失
  - 渲染在赛道表面之上（`drawTrack` → `drawBrakeMarks` → 赛车）
  - 仅当赛车在赛道上且速度 > 50 时产生
- 火花：碰撞时产生，生命周期 0.2-0.6 秒

### HUD

- **左侧面板**: 排名、比赛时间、单圈时间、速度、档位、圈数
- **右侧面板**: 其他车辆的实时数据
- **底部**: 难度、焦点、操作提示
- **结束面板**: 获胜者、排名、总用时

### 相机系统

- **全部聚焦**: 所有赛车的中心点 + 平滑跟随（阻尼系数 0.09）
- **单目标聚焦**: 平滑跟随指定车辆
- **动态缩放**: 全部聚焦模式下，计算所有赛车的包围盒并动态调整缩放值，确保所有赛车始终可见
  - 缩放值在 `ZOOM=0.85`（单目标）到 `0.15`（最大拉远）之间平滑过渡
  - **双极低通滤波**：先对 `targetZoom` 做一级平滑（系数 0.05），再对 `currentZoom` 做二级平滑（系数 0.10），消除段落感
  - 包围盒四周保留 20% 屏幕边距，保证赛车不贴边
  - 单目标聚焦时固定使用 `ZOOM=0.85`
- **双相机系统（双人分屏）**: `cam`（P1 左半屏）和 `cam2`（P2 右半屏）独立追踪各自目标
  - 半屏聚焦公式：`cam.x = target.x - W/(4*ZOOM)`
  - 使用 `renderView(ox, oy, vw, vh, camObj, zoomVal, camPlayer)` 渲染通用函数，内部 `ctx.save/clip/restore` 实现视口隔离
- **焦点循环（双人模式）**: 分屏 → 全局单视图 → 各车辆追踪 → 回到分屏，`dualSplit` 布尔值控制渲染模式

### 小地图（Minimap）

- **位置**: 右下角（`W-174, H-224`），140×120px 半透明背景
- **绘制内容**: 赛道区域（外缘/内缘/中心线）+ 赛车位置点
- **赛车点绘制**: 外层 4.5px 圆 = `c.color`（车身色），内层 2.5px 圆点 = `c.accent`（翼/条纹强调色）
- **当前玩家高亮**: P1（和 P2 双人模式）带白色 2px 边框；AI 车辆带深色 1px 边框
- **坐标系缩放**: 使用预计算 `trackBounds`（赛道世界坐标范围）将世界坐标映射到小图像素坐标

---

## 10. 开发流程与演进历史

### v0 — 原型

- 基础赛道生成（Catmull-Rom 样条）
- 玩家操控（方向盘、加减速）
- 基础渲染（简易赛道 + 矩形车身）

### v1 — 基本 AI

- 添加纯追逐 Pure Pursuit 转向
- 曲率速度剖面预计算
- AI 跟随中心线行驶
- 基础圈数检测

### v2 — 双人模式、AI 扩展

- 双人同屏控制
- 多 AI 支持（#44 / #77）
- 完整碰撞系统
- 发车格倒计时
- HUD 面板

### v2.1 — AI 稳定性、焦点切换

- 相机焦点切换功能
- AI 漂移误差系统（不同等级不同误差）
- 刹车灯
- 发车格使用完整车体渲染

### v2.2 — 弯道能力进化

- AI 过弯从中心线跟随 → 赛车线偏移（曲率 × 8000 + Gaussian 平滑）
- **问题**: AI 仍然像撞墙一样滑出
- **解决**: 移除 AI 强制恢复逻辑，使用与玩家相同的 off-track 减速 + 重设

### v2.3 — 预判转向引入

- **问题**: AI 到弯道边缘才修正，太晚
- **解决**: 3 秒前瞻预判转向 + 车身边缘碰撞检测
- 预测点距离阈值提升至车身半宽 20px

### v2.4 — 反打技术（Scandinavian Flick）

- **问题**: 急弯仍然不自然，无法流畅入弯
- **解决**: Phase 1 反打（向外摆）+ Phase 2 入弯的 out-in-out 技术
- 反打方向：左弯靠右 / 右弯靠左

### v2.5 — 自适应急弯

- **问题**: > 90° 急弯反打不足，入弯转向不够
- **解决**: 速度剖面扫描检测弯急程度，自适应调整反打幅度和转向增益
- 激进入弯模式压制安全系统干扰

### v2.6 — 视觉效果

- 高速尾部气流渐变效果（`drawWake`）
- 入弯路径可视化（淡入淡出）
- 入口路径改为入弯后显示（Phase 2），而非反打阶段显示（Phase 1）

### v2.7 — 动态缩放

- **问题**: 全部聚焦模式下，赛车相距很远时无法同时看到两辆车
- **解决**: 动态缩放根据赛车包围盒自动调整缩放值
- 包围盒保留 20% 边距，确保所有赛车在视野内
- **双极低通滤波**平滑缩放（先平滑 targetZoom 系数 0.05，再平滑 currentZoom 系数 0.10），消除段落感

### v2.8 — 出弯跟踪（Track-out）

- **问题**: AI 冲出弯心后缺少出口目标，只能依赖中心线跟随出弯，出弯速度损失大
- **解决**: 弯心过后增加出弯跟踪阶段，瞄准外侧出口目标
- 出口目标位于弯心后 25 个采样点、向赛道外侧偏移 35%
- 出弯增益 0.6-1.2（弯越急增益越高），低于入弯增益（2.2）以保证平顺
- 与弯心瞄准共享 bestApexIdx 和 insideDir，无缝衔接

### v2.9 — 刹车痕迹

- **问题**: 刹车时缺少视觉反馈
- **解决**: 所有赛车刹车时在赛道上留下黑色印记
- 印记持续 5 秒后逐渐消失，间距 15px 避免过密
- 渲染在赛道表面和赛车之间，共 3000 条上限
- 配合现有的尾灯刹车灯，提供完整的刹车视觉反馈

### v3.0 — 弯道边缘余量优化

- **问题**: AI 转弯时一直卡在跑道边缘，没有安全余量
- **解决**: 降低反打摆动阈值，留出约半个车宽的安全距离
- `swingThreshold` 从 `0.32→0.50` 降低到 `0.18→0.36`（缓弯→急弯）
- 边缘安全网阈值从 `40%/48%` 收紧到 `38%/44%`
- 急弯最大摆动从赛道边缘（50%）改为 36%，保留 ~10px（半车宽）余量

### v3.1 — 赛车配色 + 车队涂装

- **问题**: 赛车配色单调丑陋
- **解决**: 采用真实 F1 车队配色方案
  - p1 #16: Ferrari 红 `#dc0000` + 金 `#ffd700`
  - p2 #01: Mercedes 青 `#00d2be` + 银 `#c0c0c0`
  - a1 #44: Red Bull 蓝 `#1e3a5f` + 红 `#ff1e00`
  - a2 #77: McLaren 木瓜橙 `#ff7300` + 深蓝 `#002147`

### v3.2 — 5 秒路径预测 + 双轨刹车痕 + BOSS 极速 400

- **问题**: AI 预见性不足（仅 3 秒）、刹车痕只有单线、BOSS 极速过高
- **解决**:
  - 前瞻从 3 秒增至 5 秒，扫描范围从 `track.length × 0.3` 扩至 `× 0.35`
  - 刹车痕改为左右后轮双轨，间距 15px，独立跟踪左右轨迹
  - 边缘安全网新增后轮位置检测（`rt`/`lt`），确保轮胎不滑出赛道
  - BOSS 极速上限从 562.5 降至 400（硬上限 `Math.min(..., 400)`）
    - HELL 也受 400 上限约束，BOSS 优势体现在过弯速度而非极速

### v3.3 — 分屏双人 + 小地图 + 中文界面

- **双人分屏模式**: 使用 `renderView()` 通用函数结合 `ctx.save/clip/restore` 实现视口隔离
  - 左半屏追踪 P1（`cam`），右半屏追踪 P2（`cam2`）
  - 半屏聚焦公式：`cam.x = target.x - W/(4*ZOOM)`
- **小地图**: 右下角半透明赛道缩略图，赛车点使用双色涂装（车身色 + 强调色）
  - 当前玩家白框高亮，AI 深色边框
  - 使用 `trackBounds` 缩放世界坐标到地图像素
- **中文界面**: 全界面中文显示（速度→公里/时、单圈时间、比赛时间、档位、排名等）
- **焦点循环增强**: 双人模式焦点按 分屏 → 全局 → P1 → AI#44 → AI#77 → 分屏 循环
  - `dualSplit` 变量控制分屏/单视图切换
  - 聚焦指示器在分屏模式隐藏，单视图模式下显示

---

## 关键技术决策

1. **为什么用 Pure Pursuit + 速度剖面而非机器学习？**
   Pure Pursuit 可预测、可调试、确定性高。速度剖面（前进-后退扫描方法）是赛车游戏事实标准。

2. **为什么没有赛道围墙？**
   赛车滑出赛道后应自然滑动而非撞墙反弹，保持真实感。off-track 效果与玩家完全一致。

3. **为什么反打分成两阶段？**
   模拟真实赛车中「先向外摆→再切入弯心」的入弯技法。单阶段无法完成 out-in-out 路线。

4. **为什么急弯要压制安全系统？**
   边缘安全网在急弯中会干扰入弯转向。只要 off-track 系统能兜底（55% + 1s 重设），安全系统不应妨碍激进驾驶。

5. **为什么赛车线预计算但 AI 不用？**
   AI 已被设计为中心线追踪以保持一致性。赛车线保留在代码中作为参考/备用。

---

## 启动方式

任何 HTTP 服务器均可：

```bash
python3 -m http.server 8080
# 或
npx serve .
```

浏览器打开即可游戏。
